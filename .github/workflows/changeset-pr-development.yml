name: Generate Changeset PR (Development)

on:
  push:
    branches: ["main"]

permissions:
  contents: write
  pull-requests: write

jobs:
  version-pr:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"

      - name: Install root + workspaces
        run: npm install

<<<<<<< HEAD
      - name: Run Changesets
        uses: changesets/action@v1
        with:
          publish: "false"
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN_FOR_CHANGESETS }}
=======
            - name: Run Changesets
              uses: changesets/action@v1
              with:
                  publish: "false"
              env:
                  GITHUB_TOKEN: ${{ secrets.TOKEN_FOR_CHANGESETS }}
>>>>>>> efcd68d15f7c1fbb9839072d5bca1106f55a4963
